# ============================================
# Production Environment Configuration
# ============================================
# This file is a template for production settings
# Copy to .env.production and update with your values
# IMPORTANT: Use strong, unique values in production!
# ============================================

NODE_ENV=production
PORT=3000

# Database (production instance)
# Use connection pooling for production
DATABASE_URL=postgresql://user:strong-password@prod-db.example.com:5432/accounting_prod

# Supabase (production project)
SUPABASE_URL=https://your-production-project.supabase.co
SUPABASE_ANON_KEY=your-production-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-production-service-role-key-handle-with-care

# JWT (production - use strong secret!)
# Generate with: openssl rand -base64 48
JWT_SECRET=your-production-jwt-secret-must-be-at-least-32-characters-long
JWT_EXPIRATION=7d

# CORS (production domains only - NO WILDCARDS!)
CORS_ORIGINS=https://app.example.com,https://admin.example.com

# Redis (production instance with authentication)
REDIS_HOST=prod-redis.example.com
REDIS_PORT=6379
REDIS_PASSWORD=your-strong-redis-password

# Frontend (production URL)
FRONTEND_URL=https://app.example.com

# Logging (warn/error only in production)
LOG_LEVEL=warn

# ============================================
# Production Security Checklist
# ============================================
# ✅ JWT_SECRET is at least 32 characters
# ✅ JWT_SECRET is unique to this environment
# ✅ DATABASE_URL uses strong password
# ✅ REDIS uses password authentication
# ✅ CORS_ORIGINS does NOT contain wildcard (*)
# ✅ LOG_LEVEL is set to 'warn' or 'error'
# ✅ All secrets are stored securely
# ✅ Secrets are rotated regularly
# ============================================
