# ============================================
# Environment Configuration Example
# ============================================
# Copy this file to .env and fill in the values
# DO NOT commit .env files to version control!
# ============================================

# --------------------------------------------
# Application Environment
# --------------------------------------------
# Options: development, staging, production, test
NODE_ENV=development

# Server port (default: 3000)
PORT=3000

# --------------------------------------------
# Database Configuration
# --------------------------------------------
# PostgreSQL connection string
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://postgres:password@localhost:5432/accounting_db

# --------------------------------------------
# Supabase Configuration
# --------------------------------------------
# Your Supabase project URL (found in Project Settings > API)
SUPABASE_URL=https://your-project-id.supabase.co

# Supabase anonymous/public key (safe to expose)
SUPABASE_ANON_KEY=your-anon-key-here

# Supabase service role key (NEVER expose this in client-side code!)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# --------------------------------------------
# JWT Authentication
# --------------------------------------------
# Secret key for JWT token signing
# IMPORTANT: Must be at least 32 characters long!
# Generate with: openssl rand -base64 32
JWT_SECRET=your-jwt-secret-key-minimum-32-characters-long

# JWT token expiration time
# Format: <number><unit> where unit is s (seconds), m (minutes), h (hours), d (days)
# Examples: 1h, 7d, 30m, 1s
JWT_EXPIRATION=7d

# --------------------------------------------
# CORS Configuration
# --------------------------------------------
# Comma-separated list of allowed origins, or * for all origins
# Examples: http://localhost:3000,https://example.com
# WARNING: Use * only in development!
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# --------------------------------------------
# Redis Configuration (for BullMQ queues)
# --------------------------------------------
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# --------------------------------------------
# Frontend Configuration
# --------------------------------------------
# Frontend application URL (used for email verification links, password reset, etc.)
FRONTEND_URL=http://localhost:3000

# --------------------------------------------
# Logging Configuration
# --------------------------------------------
# Options: debug, info, warn, error
# - debug: Detailed logging (development only)
# - info: General information (default)
# - warn: Warning messages
# - error: Error messages only
LOG_LEVEL=info

# ============================================
# Security Reminders
# ============================================
# 1. Never commit .env files to version control
# 2. Use strong, unique secrets in production
# 3. Rotate secrets regularly
# 4. Use different values for staging/production
# 5. Keep JWT_SECRET at least 32 characters
# 6. Don't use wildcard CORS in production
# ============================================
