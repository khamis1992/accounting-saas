[phases.build]
cmds = ["cd backend && npm install && npm run build"]

[phases.start]
cmds = ["cd backend && npm run start:prod"]

[build]
cmd = "cd backend && npm install && npm run build"

[start]
cmd = "cd backend && npm run start:prod"

[build.env]
NPM_CONFIG_PRODUCTION = "false"

[statics]
# Point Nixpacks to backend directory for detection
dir = "backend"
